class Auth {
	login = (email, password) => {
		return Promise.resolve();
	};

	logout = () => {
		return Promise.resolve();
	};

	register = (name, email, password) => {
		return Promise.resolve();
	};
}

export default Auth;
